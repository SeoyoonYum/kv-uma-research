# DECISIONS.md — 결정 로그 (날짜 / 결정 / 이유)
2026-06: 주제 = 단일 GPU/통합 메모리 KV 캐시 결정 정책. 데이터센터 KV-perf는 포화로 확인(3회 검증).
2026-06: 제외 — 서빙 보안(흥미 없음), 측정-온리(노벨티 부족), 데이터센터 GB200 피벗(접근 불가·붐빔).
2026-06: 프레이밍 = 통합 메모리 스펙트럼, 단일 풀 극한에 집중, NVIDIA는 동기+대조로만.
2026-06: 플랫폼 = M4 16GB. Phase 1 측정-퍼스트 + Go/No-Go 게이트.
2026-06-20: Exp1 prefill = BODY-ONLY 측정 확정(transformer body만; lm_head/vocab projection 제외). 이유: KV 재계산 원시비용에 vocab projection 무관 + N=8192서 logits (1,8192,vocab) ≈2.5GB 폭발 회피.
2026-06-20: 발열 로깅 — pmset CPU_Speed_Limit는 Apple Silicon 미지원(Intel 전용)으로 확인 → 측정 스프레드(max/median, throttle_ratio)를 무-sudo throttle 신호로 사용. powermetrics(sudo)는 Exp2용 opt-in(PowerMetricsLogger).
2026-06-20: 7B sweep은 N≤4096로 cap(cooldown 12s). 이유: 7B@8192 prefill ≈64s/forward로 팬리스 스로틀이 prefill을 가짜로 부풀려 UMA-vs-PCIe 격차를 왜곡 → 클린 점만 채택.
2026-06-20: 크로스오버 결론 — UMA-vs-PCIe 차이는 resident 경로가 아니라 *eviction 경로*(메모리 압박)에 존재. UMA는 swap 탈출구 부재로 recompute 강제 → 가설 지지, 격차가 모델 크기로 강화(395×→1,587×).
2026-06-21: 리포 구조화 — kv-uma-research/ 부트스트랩(RESEARCH/EXPERIMENTS/DECISIONS/RELATED_WORK + src/common 리팩터링). exp1 공용 로직을 common/{measure,thermal,models,workloads}로 추출, exp2/3/4는 스켈레톤. exp1 재실행(smoke) 검증 통과, 기존 측정 데이터는 results/로 이관.
2026-06-21: Exp2(경합) — CPU 대역폭 부하(STREAM duty-cycle, P-코어 QoS)가 GPU decode를 ~36–38% 느리게(N=2048/8192, ~69 GB/s, 단조). 메커니즘 powermetrics A/B로 확정: 부하 시 GPU 클럭 유지/상승(1465MHz)·전력↑인데 throughput↓ → 대역폭/메모리 중재 경합, 전력·발열 throttle 아님. (M4 P-코어 1개가 ~92 GB/s 포화 → 강도축은 스레드수 대신 duty-cycle%.)
2026-06-21: Exp2 측정 버그 정정 — 첫 sweep(2.5–11.5%)은 측정창 직전 prefill이 CPU 부하 수명을 잠식(큰 N서 부하 조기 만료)해 과소측정. 캐시를 부하 전 빌드하도록 수정 → 정정값 ~36–38%, 프로브와 일치. ("짧은 컨텍스트가 더 민감"도 이 버그 아티팩트였음.)
2026-06-21: H2 판정 정정 — 버그값 근거 "경합 약함"은 철회. H2 강하게 지지(~38% 실효). 단 N* *이동*은 prefill-under-contention 미측정이라 미결(decode·prefill 둘 다 같은 버스 → 상대 민감도 필요). Go/No-Go = GO(H1 강 + H2 강).
2026-06-21: powermetrics passwordless sudo를 /usr/bin/powermetrics 한정으로 설정(/etc/sudoers.d/powermetrics). GPU 클럭/residency/전력 트레이스용(thermal.PowerMetricsLogger). 제거 가능.
2026-06-21: Exp2 N*-이동 닫음(exp2_prefill_contention.py) — prefill(recompute)은 경합에 거의 무반응(−0.5% @N2048, +7.5% @N4096; compute-bound, 가중치 N토큰 재사용·산술강도 높음)인데 decode(keep)는 ~37–39%(bandwidth-bound, 토큰마다 전체 가중치+KV 재독). 비대칭 → 경합 시 keep이 recompute 대비 ~1.3–1.4× 상대적 비쌈 → **N\*가 recompute 쪽으로 이동(directionally 확정)**. 정책 레버: 버스 경합 시 recompute 선호 — PCIe 모델에 없는 UMA 고유. 단 1.3–1.4×는 Exp1 ~600–1,587×에 비해 작아 지배 비대칭은 안 뒤집음(refine). H2 3다리(실재·대역폭·비대칭) 완성.
2026-06-21: 프레이밍 확정 — 논문 thesis = "단일 풀 UMA의 KV 관리 결정 구조를 *처음으로 통제·메커니즘 수준에서 측정*하고 결정 모델로 *재유도*". NOT "eviction이 비싸다는 발견"(그건 folklore).
2026-06-21: 노벨티 판정 (제출 전 게이트) —
  ① eviction=강제 recompute(600~1600×)는 정성적으로 folklore다. 다수 블로그(backend.ai, touchdown-labs, min.io)가 현상을 논하고, touchdown-labs는 16/32GB 통합 메모리 Mac + MLX로 우리 실험 셋업을 그대로 제안. arXiv 2605.05699 "When Quantization Is Free"는 UMA에서 bandwidth-driven 비용 역전을 보였으나 *quantization*에 대해서.
  ② Apple Silicon에서 decode 중 CPU/GPU 공유 버스 경합 측정 + recompute 면역 비대칭 = 미발표. 가장 가까운 학술 논문(arXiv 2501.16909)은 NVIDIA GPU 내부 멀티테넌시(다른 setting). → ②가 가장 깨끗한 노벨티.
  → 프레이밍 반전: ②(경합 비대칭)를 전면 empirical로, ①은 "folklore의 첫 통제 정량화 + 비자명한 모델-크기 스케일링(329→1587×)"으로 포지셔닝. folklore 소스를 명시적으로 인용하고 "첫 체계적·통제·메커니즘 측정 + 비용 모델"로 차별화.
2026-06-21: 결과 — 측정만으론 folklore-노출. Exp3(정책 격차)+Phase2(정책 구현) 중요도 상승. 정책이 "folklore 확인"을 "기여"로 전환.
2026-06-21: arXiv/Semantic Scholar 알림 설정 필요("unified memory KV cache", "Apple Silicon LLM inference", cs.DC/cs.LG). 제출 직전 노벨티 재검증(경쟁 가능 — 동일 셋업이 블로그에 공개됨).
2026-06-21: 베뉴 = EuroMLSys 1순위 / MLArchSys 2순위. Phase 1 = 측정 논문.
2026-06-22: Exp3 인프라 구축·검증(exp3_policygap.py + workloads.py; 측정 전용 시뮬, MLX 불필요 — Exp1 비용곡선을 비용모델로 채점). 합성 트레이스 + 동시성(메모리 압박) sweep 첫 결과: 현행 정책은 각각 ≥1축 실패 — rotating(mlx-lm 기본) ctx ~6% 무성 손실 + 극한 압박 OOM 79%; full_keep OOM 0→6→40→79%(압박↑); always_recompute 지연 ~1.79× 과다. **oracle만 0% OOM + full ctx + 최소 지연**(예산 초과 턴만 recompute) → full_keep↔always_recompute 보간. 격차 = UMA-native 적응 정책 헤드룸 → Phase2 동기. (합성 트레이스·offline 상한 oracle — 실 ShareGPT/LMSys로 magnitude 정련 남음.)
2026-06-22: Exp3 위상 승격 결정 — oracle만으로는 "상한 그림"이라 약함(리뷰어: "예지 있어야만 되면 달성 가능한가?"). online causal 휴리스틱 1개를 추가해 "격차 존재(oracle) + 예지 없이 상당 부분 회수(causal)"로 *달성 가능성*을 입증 → Exp3를 측정 논문의 결과로 승격(Phase2 동기로만 두지 않음).
2026-06-22: causal 설계 확정 — oracle과 *동일한 비용 함수*, 입력 정보만 다름(미래 재사용 모름). 점수: drop_gain(seq)=keep_cost − P_reuse×recompute_cost. P_reuse는 최근성 근사(첫 버전 계단함수). 핵심: LRU(오래된 것만)에 *recompute 비용 축*을 더한 것 = Exp1의 "recompute는 N에 superlinear" 발견을 정책으로 이전. 첫 버전은 최대 단순(정교화 금지) — 목표는 oracle 격차의 일부 회수로 달성 가능성 입증.
2026-06-22: 해석 합의 — causal이 oracle 격차의 30~60% 회수면 강한 결과. 거의 못 잡으면 그것도 발견("이 결정엔 미래 정보가 본질적 → Phase2는 예측에 투자"). 어느 쪽이든 다음 수 결정.
2026-06-22: Exp3 causal 구현·실측(실 ShareGPT 5k convs, data/raw/old/sg_52k.json 1GB·gitignored). 다중 시퀀스 eviction 모델(가변 think-time 스케줄)로 재구성. 결과: oracle 14.96 < causal 15.14–15.51 < lru 15.71 < always_recompute 16.46 s TTFT. **causal(예지 없음)이 LRU→oracle 격차의 44–75%(K=8–32), always_recompute→oracle의 72–88% 회수 → 달성 가능성 입증(강).**
2026-06-22: ★ spec 2곳 수정(챗 OK 필요) — 계단함수가 causal<LRU(음수 recovery) 만들어 정합성 수정. ① oracle P_reuse="ever-reused"(이진), "K턴 내" 아님 — Exp1상 keep<recompute 항상이라 재사용될 건 전부 유지·끝난 것만 evict가 진짜 상한(이전 oracle<LRU 버그 해소). ② causal P_reuse=exp(−idle/K) smooth(계단 0.1 floor가 오래된 큰 conv evict 막아 작은 것만 evict→causal<LRU). 둘 다 동일 drop_gain=keep_ms−P_reuse×prefill(N)(keep_ms=KV/92GB·s), P_reuse 입력만 다름 — 검증점 충족. causal은 여전히 1-param·recency-only·예지 없음.
2026-06-22: 주의 — budget 2GB/동시성16은 실 long-tail conv(최대 149k tok=4.3GB)엔 빡빡 → 절대 TTFT(~15s)는 고압박 regime. recovery%가 robust headline; budget/동시성 sweep으로 강건성 확인 필요(다음). rotating(mlx-lm 기본)은 실 multiturn서 ctx 31%(69% 손실)·full_keep peak 359GB로 둘 다 비현실적.
2026-06-22: vLLM-on-Mac 등장 확인 — (a) vllm-metal: Docker×vLLM 공식 플러그인, MLX 백엔드, 단일 풀 제로카피 + paged attention, v0.2.0(2026/04)에서 unified paged varlen Metal 커널 기본(TTFT 83×, throughput 3.6× vs v0.1.0). (b) vllm-mlx: 별개 커뮤니티 프로젝트, continuous batching + paged KV + prefix cache + SSD 티어링(EuroMLSys'26 계열).
2026-06-22: 노벨티 영향 판정 — 이들은 PagedAttention *메커니즘*을 데이터센터에서 *이식*했을 뿐, (i) 단일 풀 비용 구조 *재유도*(offload 퇴화 → keep-vs-recompute), (ii) CPU/GPU 버스 경합 비대칭은 미탐. KV 정책은 vLLM 그대로(LRU/prefix/recompute-default). SSD 티어링은 디스크 축이지 통합 메모리 내부 recompute-vs-read 축 아님. → 우리 지적 델타(비용 재유도 + 경합 비대칭 + 정책 반영) 유지·강화. "메커니즘 빈틈"은 닫혔고 "정책/비용모델 빈틈"은 열려 있음.
2026-06-22: 프레이밍 강화 — "vLLM조차 Mac에 왔다 = 통합 메모리가 진지한 플랫폼"(동기 강화) + "그 vLLM조차 PCIe 가정 정책을 이식만 함"(델타 날카로워짐). ②(경합 비대칭) 전면 프레이밍 유지.
2026-06-22: 우리가 만드는 것 정의 — vllm-metal보다 빠른 커널/배칭/인프라가 아님(그들이 잘함). *그들이 무시하는 비용 축*에서 이기는 KV 관리 *결정 정책*. 주장 = "vllm-metal의 이식된 정책은 단일 풀에서 차선, 우리 UMA-native 정책이 같은 엔진 안에서 이를 이긴다"(OOM/크래시↓ + 컨텍스트 보존 + 불필요 recompute↓, oracle 상한 근접).
2026-06-22: Phase 2 구현 전략 = 단계적 이중 트랙. Phase 2a(먼저, mlx-lm): 정책 구현·아이디어 검증(단일 사용자·멀티턴), 빠름·통제, 워크샵용 충분. Phase 2b(조건부, vllm-metal): 2a 강결과 + 랩 합류 시, vllm-metal KV 경로에 이식해 "실 서빙 엔진서 배칭 하에 그들의 LRU를 이긴다" 입증(메인 트랙 결정타). 위험 격리: vllm-metal 인프라에 먼저 빠지지 말 것 — mlx-lm로 "되는지" 먼저 싸게 확인.
2026-06-22: watch list 추가 — vllm-metal/vllm-mlx repo 릴리스 노트(빠르게 변함·경쟁: 다음 버전서 UMA-aware eviction 넣으면 ②까지 닿을 수 있음). arXiv 알림과 함께 제출 직전 재검증.
2026-06-22: vllm-metal 실 엔진 측정 보류 — docker 미설치 + Docker-on-Mac은 Metal GPU 패스스루 불가(Linux VM) + 네이티브 설치는 별프로젝트(챗 토끼굴 경고 준수). 시뮬레이션상 lru baseline이 vLLM eviction(LRU/recompute-default)의 proxy로 대조군 커버; 실 엔진 측정은 Phase 2b 인접으로 큐잉.
2026-06-22: Exp3 recovery budget robustness 확인 — 2GB(44–75%)·6GB(41–83%) 모두 causal이 LRU→oracle 격차의 ~40–83%(K=8–32) 회수. always_recompute→oracle은 79–94%. 헤드룸·달성가능성 강건(고압박일수록 절대 TTFT↑이나 recovery% 일관).
2026-06-22: ★ formulation 수정 2건 승인 (계단함수가 causal<LRU 음수 recovery를 만든 정합성 문제 해결). ① oracle P_reuse="ever reused"(이진), NOT "K턴 내" — Exp1상 keep은 항상 recompute보다 싸므로 진짜 상한=재사용될 건 전부 유지·끝난 대화만 evict(이전 "K턴 내"는 oracle<LRU 모순=무효; oracle<LRU 정합성으로 검출=올바른 디버깅). ② causal P_reuse=exp(−idle/K) smooth, NOT 계단 — 계단 0.1 floor가 오래된 큰 conv 안 비워 작은 것만 evict→recompute 누적→causal<LRU; smooth면 오래된 건 P_reuse→0(LRU처럼 evict 가능), recompute 축은 동급 recency 중 비싼 걸 보호(여전히 1-param·recency·예지 없음). 둘 다 동일 drop_gain=keep_ms−P_reuse×prefill(N), 차이는 P_reuse 입력뿐(causal=과거 recency, oracle=미래 ever-reuse), recompute=Exp1 prefill 룩업 → 검증점 충족. ※ spec 위반 아닌 spec 디버깅 — naive 점수 실패 원인(작은 conv 우선 evict로 recompute 누적)과 이 유도가 맞는 이유(keep-ms 뺄셈 + ever-reuse 상한 + smooth recency = Exp1 superlinear 통찰의 정책화)를 논문 방법론으로 명시(유도 과정 자체가 기여).
2026-06-22: K 단일 선택 안 함 — recovery-vs-K 곡선이 결과. 본문 강조=K=8~16(작고 현실적 recency 창서 이미 "강함" 41~66%). K=32(75~83%)는 곡선 상단이나 "큰 recency 창"(≈full-keep 수렴) 단서. 작은 K 회수가 달성가능성 핵심 메시지.
2026-06-22: vllm-metal 실 엔진 측정 보류 확정(Code 판단 승인: Docker-Metal 패스스루 불가 + 네이티브=토끼굴). 단 시뮬 LRU를 "vLLM proxy"로 단정 금지 — 논문엔 "현행 엔진의 recency-기반 eviction을 LRU로 *대표*"로 약하게. 실 측정은 Phase 2b future work(클라우드 Linux+vLLM은 PCIe라 부적합 → Mac 네이티브 필요).
2026-06-22: 마무리 측정 — (a) 7B@8192 prefill 54s(min 50s) but powermetrics상 GPU 854–997MHz(vs 1465 max)로 throttle 확인 → 팬리스 대형-N unthrottled 측정 불가; 앞 누적 sweep의 7B@4096(29.8s)은 thermal-inflated(클린 20.9s) → 대형-N 절대값 caveat. (b) 경합 크기 sweep(0.5/3/7B@N2048): 풀부하 슬로다운 ~38–44%로 거의 **크기-무관**(0.5B43.8·1.5B37.6·3B41.6·7B43.2%) → "큰 모델일수록 민감" 가설 미지지. 중간부하(~25GB/s)선 작은 모델이 더 민감(0.5B29>3B19>7B12.5%; 짧은 decode 스텝이 간헐 stall에 더 노출). KV-대역폭-vs-크기 부수발견은 N=2048(가중치 읽기 지배)선 경합 민감도로 안 옮겨감. 관찰로 기록·토끼굴 금지(챗 지시).
2026-06-22: Phase 2a 후킹 정찰 완료 — mlx-lm 0.31.3 `LRUPromptCache`(시퀀스 prefix 캐시+LRU eviction via `_lru.pop`)가 후킹 지점. causal=서브클래스해 victim 선택을 drop_gain(entry.nbytes=keep, prefill(len)=recompute, recency=P_reuse)으로 교체. 모든 입력이 eviction 사이트에 존재. policy/README.md에 계획 기록. baseline LRU=문자 그대로 mlx-lm LRUPromptCache("현행 recency eviction 대표"가 정확).
2026-06-22: 7B 대형-N thermal caveat 채택 — 상대값(recovery%·crossover 방향·경합 비대칭)을 헤드라인(thermal-robust, 같은 조건 비교라 inflation 상쇄). 대형-N *절대값*(600~1600× gap·recompute tax)엔 "팬리스 throttle→상한 추정, powermetrics 클럭 저하(854~997 vs 1465MHz) 문서화" caveat. "팬리스 thermal inflation"을 방법론 기여로 별도 문단(클린 7B@4096=20.9s vs 누적 sweep=29.8s, 42% inflation). 팬 있는 Mac 대형-N 교차검증=future work.
2026-06-22: 경합 크기-무관 채택 — 풀부하 슬로다운 ~38~44%가 0.5B~7B robust → H2를 "경합 비대칭은 크기-robust 구조적 효과(모델별 튜닝 불필요)"로 긍정 프레이밍. 부수가설 미지지는 일반성으로 정직 보고. 중간부하서 작은 모델 더 민감(짧은 decode 스텝)은 한 문장 관찰.
2026-06-22: Phase 2a 착수 결정 — 후킹 확정·알고리즘 검증·off-ramp 명시 → 워크샵 격상 업사이드. Phase 1=완성된 안전판. Phase 1 워크샵 draft 병렬 작성(전략 챗). UCSD 가닥 미정이라 어디로 가든 자산되게 draft 우선.
2026-06-22: Phase 2a 구현 완료 — policy/causal_cache.py(mlx-lm LRUPromptCache 서브클래스, eviction victim을 drop_gain으로 교체), policy/exp_phase2a.py(실 모델 멀티턴 replay 하니스). 캐시 스모크 통과(victim=argmax drop_gain, LRU와 다름). 실-모델 통합 동작 확인(6 convs, crash 없음). 트라이 키는 hashable 필요(mlx Model unhashable→_MKEY).
2026-06-23: ★ Phase 2a 실측 결론(OFF-RAMP) — causal이 실 1.5B 모델(25 convs, 동시성10, 0.4GB 압박)에서 LRU를 *못 이김*, K 키울수록 더 나빠짐(K=12/24/48/96 → TTFT +60/−66/−123/−174%, recompute 49/60/69/88 vs LRU 22). 진단(가설 역전): 큰 K→P_reuse→1→drop_gain이 −recompute 지배→최소-recompute(작은) conv evict→메모리 조금만 확보→**eviction churn**→recompute 폭증. keep_ms(0~2ms)가 recompute(수백~수천ms)에 압도돼 "메모리 확보" 항 무력→drop_gain이 "싼 거 쫓아내기"로 퇴화. LRU(순수 recency·size-agnostic·eviction당 메모리 효율↑)가 이김. 시뮬-실측 갭: 시뮬의 causal>LRU는 작은 효과(oracle 격차 ~5%, causal 우위 절대 ~2-4%)였고 실 엔진 churn에 안 살아남음=시뮬이 과대평가. **결론: 이 drop_gain 휴리스틱은 실 엔진서 LRU 못 이김(negative result). Phase 1(Exp1/2/3)=논문 안전판으로 워크샵 제출. 정책 win엔 recompute-cost 축 이상=실제 reuse 예측 필요(Phase 2b/future).** 시뮬 oracle(예지)만 LRU 크게 이겼으니 "예측이 본질적"이 발견. 벽에 머리 안 박음(챗 off-ramp 지시 준수).
